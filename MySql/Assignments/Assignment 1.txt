                                                                   Assignment 1



Q1). show databases;

Q2). create table countries(
    -> country_id int,
    -> country_name varchar(25),
    -> region_id int);

Q3). create table jobs
    -> (job_id int primary key,
    -> job_title varchar(25) not null,
    -> min_salary decimal(7,2),
    -> max_salary decimal(7,2) check(max_salary <= 25000));

Q4). create table job_history
    -> (employee_id int,
    -> start_date date,
    -> end_date date,
    -> job_id int,
    -> department_id int);

Q5). alter table countries
    -> modify column country_id int unique;

Q6). create table jobs_default
    -> (job_id int,
    -> job_title varchar(25) default "",
    -> min_salary decimal(7,2) default 8000,
    -> max_salary decimal(7,2) default null);

Q7). create table departments
    -> (department_id decimal(4,0) not null,
    -> department_name varchar(25) not null,
    -> manager_id decimal(6,0) not null,
    -> location_id decimal(4,0),
    -> primary key (department_id, manager_id));

Q8). create table employees
    -> (employee_id int primary key,
    -> first_name varchar(50),
    -> last_name varchar(50),
    -> email varchar(100),
    -> phone_number varchar(20),
    -> hire_date date,
    -> job_id int,
    -> salary decimal(10,2),
    -> commission decimal(10,2),
    -> manager_id int,
    -> department_id int);

       alter table employees
    -> add foreign key (department_id)
    -> references departments(department_id);

Q9). step 1.
       create table department_managers(
    -> department_id decimal(4,0),
    -> manager_id decimal(6,0),
    -> manager_name varchar(50) not null,
    -> primary key (department_id, manager_id));

     step 2.
       alter table employees
    -> add foreign key (department_id, manager_id)
    -> references department_managers(department_id, manager_id);










